public:: true
source-domain:: mv
alias:: [[Legacy Fixture]]

# Legacy Public

EXP-V03 (negative, ADR-2112): a page whose only "metadata" is a leading block
of `key:: value` lines has no metadata at all. The lines are body text, the
page carries no frontmatter, and it is therefore private.
